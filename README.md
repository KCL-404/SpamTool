# SpamTool

This is a Python-based automated sharing and bot tool with a secure approval key system.  
The tool is owner-controlled — only the owner can access the Admin Panel to generate/revoke keys.

## Features
- Auto Share posts on Facebook
- Add / Remove / Select cookies
- Admin Panel for creating/revoking keys (Owner only)
- Key validation with expiration or lifetime

## Setup
1. Make sure you have Python 3 installed.
2. Install required modules:
```bash
pip install requests rich